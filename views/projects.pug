extends layout

block content
  article
    h1 My Projects
    each project in projects
      -var postlink = 'projects/' + project.slug;
      a(href=postlink, class="list-item")
        strong= project.header
        span= project.subheader